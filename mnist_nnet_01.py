

# weighted sum of inputs plus a constant fed through an activation function
# different types of activation functions

# Ex: SOFTMAX(Ln)= e^L/||e^L||
# Y = SOFTMAX(X*W + b)
# Predictions = softmax(Images X[100,784] * weights W[784,10] + (each line) Biases[10])
#
